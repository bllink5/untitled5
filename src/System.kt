class System {
    fun sistemSystem(){
        val rollDice = arrayOf(1,2,3,4,5,6)
        var score = 0
        var dice = rollDice.random()
        var total = dice
        var fullScore = score + total
    }
}